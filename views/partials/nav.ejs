<nav class="navbar navbar-expand-lg sticky-top navbar-light bg-light">
	<a class="navbar-brand" href="/">My NU Home</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item<%= active == 'list' ? ' active' : '' %>">
        <a class="nav-link" href="/list">Buy <%- active == 'list' ? '<span class="sr-only">(current)</span>' : '' %></a>
      </li>
      <li class="nav-item<%= active == 'sell' ? ' active' : '' %>">
        <a class="nav-link" href="/sell">Sell <%- active == 'sell' ? '<span class="sr-only">(current)</span>' : '' %></a>
      </li>
      <% if (loggedIn) { %>
        <li class="nav-item<%= active == 'user' ? ' active' : '' %>">
          <a class="nav-link" href="/user">My Profile <%- active == 'user' ? '<span class="sr-only">(current)</span>' : '' %></a>
        </li>
      <% } %>
    </ul>
  </div>
</nav>
